/* 符号悬停词典：公式符号、shape 记号与配图标注的中文解释。
 * scopes 按课程模块分组；pageOverrides 在个别页面调整主要含义；
 */window.DLSym = (function () {
  const common = {
    '∂': ['∂（偏导）', '∂L/∂a 读作“固定其他参数不动时，L 对 a 的变化率”。多个偏导放在一起就是梯度。'],
    'Σ': ['Σ（求和）', '把下标取遍所有取值的项加在一起：Σᵢ 表示对 i = 1…n 求和。'],
    '⊙': ['⊙（逐元素乘）', '两个同 shape 张量在对应位置相乘，结果 shape 不变。代码里常写作 a * b。'],
    'ε': ['ε（epsilon）', '一个很小的常数（如 1e-5），加在分母里防止除以 0，对数值影响可忽略。'],
    'η': ['η（学习率 learning rate）', '控制每次参数更新走多远。太小收敛慢，太大可能跳过最低点、来回震荡。'],
    '≈': ['≈（约等于）', '教学示例里保留四位小数后的近似值。'],
    '⌊': ['⌊ ⌋（向下取整）', '取不大于括内数值的最大整数。卷积输出尺寸公式用它保证结果为整数格点。'],
    '⌋': ['⌊ ⌋（向下取整）', '取不大于括内数值的最大整数。卷积输出尺寸公式用它保证结果为整数格点。'],
    '√': ['√（平方根）', '注意力里除以 √d 的缩放见 “d / d_head” 条目；RMSNorm 里是均方根。'],
    '←': ['←（赋值）', '“右边的值算完后写回左边”，a ← a − ηg 就是先算梯度再更新 a。']
  };

  const scopes = {
    b1: {
      'x': ['x（输入）', '输入特征。直线拟合里是工作年限，圆形分类里是平面坐标，矩阵写法里大写为 X。'],
      'y': ['y（真实值）', '数据里的真实观测值。回归里是薪资；分类里是标签（圆内 1、圆外 0），one-hot 写成 Y。'],
      'ŷ': ['ŷ（预测值）', '模型算出的预测，读作 y-hat。ŷ = ax + b。与真实值 y 的差距就是残差。'],
      'a': ['a（斜率）', '直线的斜率，可调参数之一。task 00 训练结束后会换算回原始单位（约 9450）。'],
      'b': ['b（截距 / 偏置）', '① 标量截距：直线的常数项；② 矩阵写法里的偏置向量 b，shape (1, out_dim)，沿 batch 广播。同字母两用法，看上下文。'],
      'L': ['L（损失 loss）', '用一个数衡量预测离真实值多远。本块用到均方误差 MSE 和交叉熵；损失小 ≠ 新数据上一定好。'],
      'n': ['n（样本数）', '参与这次平均的样本个数。'],
      'i': ['i（样本编号）', '第 i 条数据。Σᵢ、xᵢ、yᵢ 里的下标。'],
      'e': ['e（自然常数 / 残差）', '① e ≈ 2.718，指数函数的底（softmax、sigmoid 里）；② 带“残差”上下文时，eᵢ = yᵢ − ŷᵢ。'],
      'm': ['m（batch 内样本数）', '圆形分类里输入矩阵的行数，X shape (m, 2)。优化器一节里另作均值符号（m_t），见对应公式。'],
      's': ['s（线性打分）', '未归一化的分数：s = w₁x₁ + w₂x₂ + b。s > 0 判圆内，s = 0 是边界。'],
      'z': ['z（线性加权和）', '激活函数的输入：z = XW + b 的一个元素。ReLU(z) = max(0, z)；sigmoid 里 σ(z) 同一个 z。'],
      'X': ['X（输入矩阵）', '一批输入放一行一个：shape (B, in_dim)。task 01 里是 (m, 2)，MNIST 里是 (B, 784)。'],
      'W': ['W（权重矩阵）', '本课程约定 shape (in_dim, out_dim)，即“输入维数 × 输出维数”。W₁/W₂/W₃ 表示第几层。'],
      'Y': ['Y（输出矩阵）', '线性层输出 Y = XW + b，shape (B, out_dim)。交叉熵里 one-hot 标签矩阵也记作 Y。'],
      'Z': ['Z（线性输出）', '激活前的加权和：Z₁、Z₂、Z₃ 是第几层的线性输出。Z₃ 即 logits。'],
      'H': ['H（隐藏层激活）', '过 ReLU 后的隐藏层输出：H₁ = ReLU(Z₁)。shape (m, 4)。'],
      'B': ['B（batch size）', '一次前向一起处理的样本数，损失通常对它取平均。'],
      'g': ['g（梯度 / 门控）', '优化器公式里 g_t 是当前批次的梯度；Block 3 的 SwiGLU 里另指门控分支。'],
      'v': ['v（速度 / 二阶矩）', 'Momentum 的速度 v_t = βv_{t−1} + (1−β)g_t；Adam 的 v_t 是梯度平方的移动平均（二阶矩）。'],
      'u': ['u（中间量）', '反向传播示例里 u = wx 的中间结果；GELU 推导里 u 是 tanh 的自变量。'],
      't': ['t（步数）', '第 t 次参数更新。g_t、v_t、s_t、m_t 的下标。'],
      'θ': ['θ（参数统称）', '优化器公式里代表“任意一个参数”（weight 或 bias），更新规则对所有 θ 一样。'],
      'β': ['β（衰减率）', '0 到 1 之间，控制历史信息保留多少：Momentum/Adam 的动量项；Adam 有两条 β₁、β₂。BatchNorm 里同名符号是可学习平移，见归一化公式。'],
      'ρ': ['ρ（RMSProp 衰减率）', '历史平方统计的保留比例：s_t = ρs_{t−1} + (1−ρ)g_t²。'],
      'p': ['p（概率 / 丢弃率）', 'softmax 公式里是某类的概率；Dropout 一节里 p 是丢弃概率、q = 1−p 是保留概率。'],
      'q': ['q（保留概率）', 'Dropout 里 q = 1 − p；inverted dropout 除以 q 保持期望不变。'],
      'P': ['P（概率矩阵）', 'softmax 输出，shape (B, 类别数)。与 softmax+交叉熵组合时梯度为 (P − Y)/B。'],
      'd': ['d（梯度前缀 / 维度）', '写在量前面表示该量的梯度：dW 读“W 的梯度”，dZ 是线性输出处的上游梯度。Adam 一节里 m_t 的上标含义见公式。'],
      'dW': ['dW（权重梯度）', '损失对 W 的梯度，dW = XᵀdY，shape 与 W 相同。优化器原地写入这个数组。'],
      'db': ['db（偏置梯度）', '沿样本轴对 dY 求和，shape (1, out_dim)。'],
      'dX': ['dX（输入梯度）', '传给上一层的梯度：dX = dY·Wᵀ。'],
      'dY': ['dY（上游梯度）', '损失传到当前层输出的梯度，backward 的入口参数。'],
      'dZ': ['dZ（线性输出梯度）', 'softmax + 交叉熵组合后 dZ = (P − Y)/B；除以 B 是因为损失对 batch 取了平均。'],
      'logits': ['logits（未归一化分数）', '每个类别的原始分数，可正可负、和不必为 1。经 softmax 才变成概率。'],
      'softmax': ['softmax（归一化）', '把一行分数变成非负、和为 1 的概率。实现时先减去行最大值防止 exp 溢出。'],
      'ReLU': ['ReLU', 'max(0, z)：负数归零、正数原样。反向时正位置传梯度、负位置传 0；z=0 处按约定取 0。'],
      'SiLU': ['SiLU', 'x·σ(x)：平滑版 ReLU，允许小幅负值通过。Block 3 的 SwiGLU 用它做门控。'],
      'GELU': ['GELU', 'x·Φ(x)，实现用 tanh 近似。比 ReLU 平滑，Transformer 里常见。'],
      'σ': ['σ（sigmoid）', 'σ(z) = 1/(1 + e⁻ᶻ)，把一个数压到 (0,1)。作为激活或二分类概率输出。'],
      'μ': ['μ（均值）', '标准化用：x′ = (x − μ)/σ。BatchNorm 里 μ_B 是当前 batch 的均值。'],
      'M': ['M（掩码 / mask）', 'Dropout 里 M_ij ~ Bernoulli(q) 是 0/1 掩码。'],
      'N': ['N（正态分布）', 'W ~ N(0, 2/fan_in)：从均值 0、方差 2/fan_in 的正态分布采样（He 初始化）。'],
      'fan': ['fan_in（输入扇入）', '该层每个输出连接的输入个数。He 初始化方差 2/fan_in，避免激活逐层放大或衰减。'],
      'MSE': ['MSE（均方误差）', '残差平方的平均：L = (1/n)Σ(yᵢ − ŷᵢ)²。平方让正负误差不抵消、大误差代价更高。']
    },

    b2: {
      'N': ['N（样本数）', '卷积张量第一维：一个 batch 里有几张图。同 Block 1 的 B。'],
      'B': ['B（batch size）', '一次前向的图片数。输入 shape (B,3,32,32) 里的 B。'],
      'C': ['C（通道数）', '特征图的通道维。输入是 RGB 三通道；卷积后每通道不再对应颜色，而是一种学到的模式。C_in/C_out = 输入/输出通道数。'],
      'H': ['H（高度）', '特征图的高（像素行数）。输出尺寸 H_out = ⌊(H+2P−K)/S⌋+1。'],
      'W': ['W（宽度）', '特征图的宽。注意与权重 W 同字母：本块公式里 W_o,c,u,v 才是权重，单独的 H、W 是空间尺寸。'],
      'K': ['K（核尺寸）', '卷积核高/宽（如 3×3 里的 3）。K_h、K_w 分别是核高、核宽。'],
      'P': ['P（补边）', '每侧补 0 的圈数。P=1、3×3 核、步长 1 时空间尺寸不变。'],
      'S': ['S（步长 stride）', '窗口每次滑动几格。S=2 会让输出尺寸约减半（本仓库用它降采样）。'],
      'X': ['X（输入特征图）', 'shape (N, C_in, H, W)（NCHW 轴序）。im2col 展开后记作 X_col。'],
      'W': ['W（卷积权重）', 'shape (C_out, C_in, K_h, K_w)：每组权重对应一个输出通道。与空间宽度 W 同字母，看下标区分。'],
      'Y': ['Y（输出特征图）', 'shape (N, C_out, H_out, W_out)。Y_n,o,i,j 的下标：第 n 张图、第 o 个输出通道、第 i 行 j 列。'],
      'b': ['b（偏置）', '每个输出通道一个标量偏置 b_o，shape (C_out,)，加在求和结果上。'],
      'c': ['c（通道编号）', 'x_n,c,h,w 的下标：第 c 个通道。'],
      'n': ['n（样本编号）', '张量第一维的下标：第 n 张图。'],
      'h': ['h（行位置）', '空间维下标，第 h 行。'],
      'w': ['w（列位置）', '空间维下标，第 w 列。与权重 W 的大小写区分。'],
      'u': ['u（核内行）', '卷积求和公式里遍历核行的下标（0…K_h−1）。'],
      'v': ['v（核内列）', '卷积求和公式里遍历核列的下标（0…K_w−1）。'],
      'i': ['i（输出行）', '输出特征图的行下标。'],
      'j': ['j（输出列）', '输出特征图的列下标。'],
      'o': ['o（输出通道）', 'Y_n,o,i,j 与 b_o 的下标：第 o 个输出通道。'],
      'X_col': ['X_col（窗口矩阵）', 'im2col 把每个感受野展成一行：shape (N·H_out·W_out, C_in·K_h·K_w)。行序 = 样本→行→列。'],
      'W_col': ['W_col（展开权重）', '卷积核展平成 (C_in·K_h·K_w, C_out)，与 X_col 做一次矩阵乘就是卷积。'],
      'Y_col': ['Y_col（展开输出）', 'X_col @ W_col + b，再 reshape 回 (N,C_out,H_out,W_out)。'],
      'dW_col': ['dW_col', '权重梯度：dW_col = X_colᵀ·dY_col，与 W_col 同 shape。'],
      'dX_col': ['dX_col', '输入梯度：dX_col = dY_col·W_colᵀ，再由 col2im 按窗口位置累加回原 shape（重叠像素贡献相加）。'],
      'dY_col': ['dY_col', '输出梯度重排成 (N·H_out·W_out, C_out) 后的形式。'],
      'y': ['y（池化输出）', 'GlobalAvgPool 里 y_n,c = 该通道 H×W 个值的平均。'],
      'HW': ['H·W（空间位置数）', '一个通道里空间位置的个数。GlobalAvgPool 对它求平均，backward 把梯度均分成 1/(HW)。'],
      'x': ['x（单张量记号）', 'BatchNorm 公式里的输入，按通道 c 统计：x_n,c,h,w。'],
      'x̂': ['x̂（标准化后）', '(x − μ_c)/√(σ_c² + ε)，均值约 0、方差约 1，再乘 γ 加 β。'],
      'μ': ['μ_c（通道均值）', '通道 c 跨 (N,H,W) 三个轴的均值；训练时用 batch 统计，eval 时用 running_mean。'],
      'σ': ['σ_c²（通道方差）', '通道 c 跨 (N,H,W) 的方差。注意 σ 也常作 sigmoid 函数名，归一化公式里是方差。'],
      'γ': ['γ（缩放参数）', 'BatchNorm/LayerNorm 里可学习的逐通道缩放，shape (1,C,1,1)。与 β 一起决定输出尺度。'],
      'β': ['β（平移参数）', 'BatchNorm 里可学习的逐通道平移。与优化器动量 β 同字母不同物。'],
      'm': ['m（EMA 权重）', 'running ← m·running + (1−m)·batch 统计。本仓库 m=0.9，是旧统计量的权重（与框架里 momentum 定义可能相反，以公式为准）。'],
      'M': ['M（位置数）', 'BatchNorm backward 里 M = N·H·W，参与两项修正项的系数。'],
      'running_mean': ['running_mean（运行均值）', '训练期间用 EMA 积累的统计 buffer。eval() 时替代 batch 统计做标准化；只存 parameters 的 checkpoint 恢复不了它。'],
      'running_var': ['running_var（运行方差）', '同上，方差的运行 buffer。'],
      'z': ['z（相加结果）', '残差块里 z = F(x) + S(x)，再过 ReLU 得 y。'],
      'F': ['F(x)（主分支）', '残差主分支变换：Conv3×3→BN→ReLU→Conv3×3→BN。它拟合的是“还需补上的变化” H(x) − x。'],
      'S': ['S(x)（shortcut）', '捷径分支：shape 不变时 S(x)=x（identity）；shape 改变时换成 Conv1×1+BN 投影。'],
      'I': ['I（单位矩阵）', 'identity shortcut 的雅可比：梯度原样通过。J_S = I 是“梯度多一条直接路径”的形式化。'],
      'J': ['J（雅可比）', '分支对输入的局部线性化矩阵。dL/dx = (dL/dy)(J_F + J_S)：两条路径的梯度相加。'],
      'L': ['L（损失）', 'L_epoch：一个 epoch 的平均损失；L_b：一个 batch 的损失。'],
      'ReLU': ['ReLU', 'max(0, z)。本仓库的残差块在两分支相加之后还有一个 ReLU（post-activation）。'],
      'BN': ['BN（BatchNorm）', '跨 (N,H,W) 对每通道统计并标准化，γ/β 仿射。训练用 batch 统计，eval 用 running buffers。'],
      'Conv': ['Conv（卷积）', '局部窗口加权和 + 参数共享。本仓库 3×3 卷积 padding=1 保持尺寸，stride=2 降采样。']
    },

    b3: {
      'B': ['B（batch size）', '一次一起处理的序列条数。'],
      'T': ['T（序列长度）', '每个序列的位置数。Q/K/V、score 都是 (T, ·) 方阵相关。'],
      'D': ['D（模型宽度）', '每个 token 的特征数，也叫 d_model。head 拆分后每头 D/H 维。'],
      'Dh': ['D_h（head 维度）', '每个 head 的特征数 = D / H。注意力分数除以 √D_h 缩放。'],
      'H': ['H（head 数 / 中间宽度）', '注意力里是 head 个数（H_q query 头、H_kv KV 头）；FFN 参数量公式里 m 或 H 是中间宽度。'],
      'pos': ['pos（位置编号）', 'token 在序列里的整数位置：0, 1, 2, …。正弦编码和 RoPE 都以它为自变量。'],
      'i': ['i（维度对编号）', '每两维组成一对：(0,1) 是第 0 对，(2,3) 是第 1 对……第 i 对的频率 ω_i = base^(−2i/D)。'],
      'PE': ['PE（正弦位置编码）', '固定表（不训练），shape (max_len, D)：偶数维放 sin、奇数维放 cos，加到 embedding 上。'],
      'ω': ['ω_i（角频率）', '第 i 对维度单位位置转过的角度 = base^(−2i/D)。i 越大转得越慢，低维编码细粒度、高维编码粗粒度位置。'],
      'θ': ['θ（频率 / 参数）', '正弦编码里 θ(pos,i) = pos/10000^(2i/D) 是角度；RoPE 里 θ_i 是第 i 对的基频；优化器上下文里泛指参数。'],
      'base': ['base（频率基数）', '10000。控制各维度对频率的跨度范围。'],
      'm': ['m（中间宽度 / 位置）', 'FFN 参数量公式里 m 是中间维（SwiGLU 取 ≈8d/3 才与普通 FFN 同量级）；RoPE 推导里 m、n 是两个位置编号。'],
      'n': ['n（位置编号）', 'RoPE 相对位移推导里的另一个位置：点积只依赖 n − m。'],
      'R': ['R_m（旋转矩阵）', '位置 m 对一对维度做的二维旋转。(R_m q)ᵀ(R_n k) = qᵀ R_{n−m} k：位置影响只看相对位移 n−m。'],
      'q': ['q（query 向量）', '当前位置“发起匹配”的向量。由输入经 W_Q 投影而来，RoPE 旋转 q、k，不旋转 v。'],
      'k': ['k（key 向量）', '每个位置“被匹配”的向量。q·k 点积衡量匹配程度。'],
      'v': ['v（value 向量）', '匹配后实际“取回的内容”，按注意力权重加权求和。'],
      'Q': ['Q（query 矩阵）', '全序列的 query，shape (B, H, T, D_h)。Q = X·W_Q。'],
      'K': ['K（key 矩阵）', '同上，被匹配方。GQA 里 K 只有 H_kv 组，被多个 query 头共享。'],
      'V': ['V（value / 词表）', '① 注意力的 value 矩阵；(B,H,T,D_h)；② LM head 一节里 V 表示词表大小。同字母两义，看位置。'],
      'X': ['X（输入 hidden states）', '(B,T,D)：进入该层的每个位置向量。Q/K/V 都由它投影。'],
      'A': ['A（注意力权重）', 'softmax(QKᵀ/√D_h + M)：每行是一组和为 1 的“读取权重”，A_t,j 表示位置 t 从位置 j 取多少。'],
      'O': ['O（注意力输出）', 'A@V 再拼接各 head、过 W_o 投影回 (B,T,D)。'],
      'M': ['M（mask 矩阵）', '加性掩码，与 score 同 shape：允许 0、禁止 −∞。causal mask 下三角全 0、上三角 −∞。必须加在 softmax 之前。'],
      's': ['s_tj（匹配分数）', '位置 t 的 query 与位置 j 的 key 的缩放点积：q_t·k_j/√D_h。'],
      'a': ['a_tj（读取权重）', 'softmax 后位置 t 分给位置 j 的权重，Σⱼ a_tj = 1。它不是“下一个 token 的概率”。'],
      'o': ['o_t（位置输出）', '位置 t 按 a_tj 加权汇总 v_j 得到的向量，已混入前文信息。'],
      'd': ['d（特征维数）', 'Q/K 每维的特征数（即 D_h）。点积方差随 d 增长，除以 √d 防止 softmax 过早饱和。'],
      'softmax': ['softmax（归一化）', '沿 key 方向把分数变成权重。被 mask 成 −∞ 的位置权重恰为 0。'],
      'sin': ['sin（正弦）', '正弦编码偶数维用 sin(pos/10000^(2i/D))；RoPE 旋转矩阵的元素。'],
      'cos': ['cos（余弦）', '正弦编码奇数维用 cos；RoPE 里 cos(mθ) 构成旋转矩阵对角元。'],
      'tanh': ['tanh（双曲正切）', 'GELU tanh 近似里的中间函数，输出 (−1,1)。'],
      'log': ['log（对数）', '交叉熵取自然对数：正确类别概率越接近 1，−log p 越接近 0。'],
      'exp': ['exp（指数）', 'e^x。softmax 的分子；实现时先减最大值防溢出。'],
      'max': ['max（最大值）', 'softmax 数值稳定版里的行最大值；top-k 采样的排序依据。'],
      'W_gate': ['W_gate / gate_proj（门控投影）', 'SwiGLU 第一条分支 D→H，输出经 SiLU 后当“音量旋钮”。'],
      'W_up': ['W_up / up_proj（内容投影）', 'SwiGLU 第二条分支 D→H，提供被放行的内容。'],
      'W_down': ['W_down / down_proj（输出投影）', '把逐元素乘积从 H 投回 D。'],
      'SiLU': ['SiLU', 'z·σ(z)。平滑且允许小幅负值（z=−1 时约 −0.27），门可以连续调节开度。'],
      'σ': ['σ（sigmoid）', 'σ(z) = 1/(1+e⁻ᶻ)。SiLU 和门控里的压缩函数。'],
      'FFN': ['FFN（前馈网络）', '逐位置独立的两层 MLP：先变宽、非线性、再变回。不跨位置交换信息，序列方向边界仍由 attention 决定。'],
      'SwiGLU': ['SwiGLU', '三条投影的门控 FFN：SiLU(W_gate·x) ⊙ (W_up·x)，再 W_down 投回。中间维取 ≈8d/3 使参数量与普通 FFN 相当。'],
      'RMSNorm': ['RMSNorm', '按均方根缩放每个位置的特征：x/RMS(x)·γ。不减均值；MiniMind 用它组 PreNorm 块。'],
      'E': ['E（embedding 矩阵）', '查表矩阵 (V, D)：一行一个 token 的向量。MiniMind 里与 LM head 共享（weight tying）：lm_head.weight is embedding.weight。'],
      'LM': ['LM head（语言模型头）', '最后的线性层：把每个位置向量变成词表分数 (B,T,D)→(B,T,V)，softmax 后才是 token 概率。'],
      'logits': ['logits（词表分数）', '每个候选 token 的未归一化分数。生成时只取最后一个位置的。'],
      'W_Q': ['W_Q / Wq（query 投影）', 'X→Q 的可训练矩阵。'],
      'W_K': ['W_K / Wk（key 投影）', 'X→K 的可训练矩阵。GQA 里只有 H_kv 组。'],
      'W_V': ['W_V / Wv（value 投影）', 'X→V 的可训练矩阵。同样只有 H_kv 组。'],
      'W_o': ['W_o（输出投影）', '拼接各 head 后的输出投影 (D,D)，回到模型宽度。'],
      'τ': ['τ（temperature 温度）', '采样前把 logits 除以 τ：τ<1 分布更尖（更确定），τ>1 更平（更随机）。只改变选择过程，不重新训练。'],
      'k_new': ['k_new（新 key）', 'decode 阶段新 token 的 key：(B,H_kv,1,D_h)，concat 到 K_cache 末尾成 K⁺。'],
      'v_new': ['v_new（新 value）', '同上，value 侧的新增量。'],
      'K_cache': ['K_cache（key 缓存）', '(B,H_kv,T,D_h)，每层各一份、不跨层共享。旧位置的 K 不受新 token 影响，故可复用。'],
      'V_cache': ['V_cache（value 缓存）', '同 K_cache，value 侧。GQA 下只有 H_kv 组，元素数比 MHA 少。'],
      'q_new': ['q_new（新 query）', '(B,H_q,1,D_h)，读取已缓存的 K/V，输出新 hidden/logits。'],
      'H_q': ['H_q（query 头数）', 'query head 的数量（示例 4）。输出始终有 H_q 个头。'],
      'H_kv': ['H_kv（KV 头数）', 'key/value head 的数量（示例 2）。GQA 让每 H_q/H_kv 个 query 头共享一组 K/V，缓存元素数按比例减少。']
    }
  };

  /* 个别页面调整主要含义：键为 slug */
  const pageOverrides = {
    'pooling-batchnorm': {
      'm': ['m（EMA 权重）', 'running ← m·running + (1−m)·batch 统计。本仓库 momentum=0.9，m 是旧统计量的权重；不同框架对 momentum 的定义可能相反，移植时以更新公式为准。']
    },
    'swiglu': {
      'm': ['m（中间宽度）', 'SwiGLU 三个矩阵的参数量 3dm；取 m ≈ 8d/3 时与普通 FFN 的 8d² 相当。LLaMA：4096×8/3≈10923，对齐到 11008。'],
      'd': ['d（模型宽度）', 'd_model，本页参数量公式里的 d；中间维 m 以它为基准（m ≈ 8d/3）。']
    },
    'embedding-lm-head': {
      'V': ['V（词表大小）', '词表里 token 的个数。LM head 输出 (B,T,V)：每个位置给每个候选 token 一个分数。与注意力里的 Value 矩阵同字母，注意区分。'],
      'E': ['E（embedding 矩阵）', '(V, D) 的查表矩阵：输入时按编号取一行，输出时 LM head 用它的转置 Eᵀ 打分。权重共享（weight tying）减少独立参数。']
    }
  };

  /* 下标注释：没有专门条目时，附在基础符号的解释后面 */
  const subGloss = {
    'i': '下标 i：样本/维度对编号',
    'j': '下标 j：位置或类别编号',
    'k': '下标 k：类别编号',
    't': '下标 t：时间步 / 第 t 次更新',
    'c': '下标 c：通道编号',
    'n': '下标 n：样本编号',
    'h': '下标 h：高度方向的格点',
    'w': '下标 w：宽度方向的格点',
    'u': '下标 u：核内的行',
    'v': '下标 v：核内的列',
    'o': '下标 o：输出通道编号',
    'out': '下标 out：输出方向',
    'in': '下标 in：输入方向',
    'col': '下标 col：im2col 展开后的矩阵',
    'B': '下标 B：当前 batch 的统计量',
    'new': '下标 new：本次新 token 的量',
    'cache': '下标 cache：缓存里保存的量',
    'gate': '下标 gate：SwiGLU 的门控分支',
    'up': '下标 up：SwiGLU 的内容分支',
    'down': '下标 down：SwiGLU 的输出投影',
    'ik': '下标 ik：第 i 个样本的第 k 类',
    'tj': '下标 tj：位置 t 对位置 j',
    'q': '下标 q：query 相关',
    'kv': '下标 kv：key/value 相关',
    'head': '下标 head：按 head 拆分后的维度',
    'model': '下标 model：整个模型的宽度（d_model）',
    'run': '下标 run：运行统计（running）'
  };

  /* shape 记号（行内代码里的轴标签） */
  const shapeGloss = {
    'B': 'B：batch size，一次一起处理的样本数',
    'N': 'N：样本数（与 B 同义，卷积惯用）',
    'T': 'T：序列长度（位置数）',
    'D': 'D：每个 token 的特征数（d_model）',
    'Dh': 'Dh：每个 head 的特征数（d_head）',
    'F': 'F：特征维数（全连接层的列数）',
    'C': 'C：通道数',
    'C_in': 'C_in：输入通道数',
    'C_out': 'C_out：输出通道数',
    'H': 'H：特征图 / 图片的高（注意力里另作 head 数）',
    'W': 'W：特征图 / 图片的宽（权重符号为大写斜体 W）',
    'K': 'K：卷积核尺寸',
    'K_h': 'K_h：卷积核高',
    'K_w': 'K_w：卷积核宽',
    'S': 'S：步长 stride',
    'P': 'P：每侧补边 padding',
    'V': 'V：词表大小',
    'H_q': 'H_q：query head 数',
    'H_kv': 'H_kv：key/value head 数（GQA 共享）',
    'Hq': 'Hq：query head 数',
    'Hkv': 'Hkv：key/value head 数（GQA 共享）',
    'd': 'd：每个 Q/K 的特征数（head 维度）；注意力分数除以 √d',
    'num_classes': 'num_classes：类别数',
    'classes': 'classes：类别数',
    'D_h': 'D_h：每个 head 的特征数',
    'H_out': 'H_out：输出特征图高 = ⌊(H+2P−K)/S⌋+1',
    'W_out': 'W_out：输出特征图宽',
    'max_len': 'max_len：位置编码表支持的最大长度'
  };

  /* 配图图例：key = 图片文件名；items = [图上标注, 含义] */
  return { common: common, scopes: scopes, pageOverrides: pageOverrides, subGloss: subGloss, shapeGloss: shapeGloss };
})();
