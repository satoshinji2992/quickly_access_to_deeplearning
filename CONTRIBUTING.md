# 教程改稿约定

改这套教程时，先回到 Block 1 的起点：读者看见几个点，想让电脑拟合一条线。损失、梯度和学习率都是为了解决眼前的麻烦而出现，不是按知识点目录依次登场。Block 2 和 Block 3 也沿用这个节奏。

## 两种页面怎样衔接

`chapters/` 是可以连续阅读的主文。它从具体例子出发，把动机、手算和必要的数学连起来。

`exercises/*/README.md` 是紧跟主文的实现说明。它不重新定义一遍概念，而是回答代码里的具体问题：数组是什么 shape，forward 要留下什么，backward 沿哪个轴求和，运行后应该看到什么。

实现页面接着主文往下走：它既解释代码，也是学习路线的下一层。

## 从问题写到实现

先给一个确定的麻烦。直线围不住圆，普通深网的训练误差反而更高，当前 token 在训练时会看见自己的答案。读者知道问题在哪里，新结构才有出场的理由。

然后用一个能手算的例子试它。公式里的符号要在前文见过；从单个样本扩展到 batch 时，再补 shape 和轴的约定。如果一段需要连续列出十几个张量，往往是前面少了一个小例子。

最后回到实现。链接真实文件，给一条可运行命令和一段可判断的结果。“程序没有报错”通常不够；一个有用的结果应该告诉读者，loss 大约会怎样变，单个 batch 能否被拟合，或者两种计算的误差应在什么量级。

## 语气

- Block 1 最初的写法是整套教程的语气基准：一个问题没解决，才自然地引出下一个工具。改稿可以修正错误和节奏，不要把它重写成一套统一的文档模板。
- 可以用“你”，也可以写“我们”。读起来要像有人在桌边把一步计算讲清，而不是接口手册。
- 不写“请完成”“提交”“打卡”“理解检查”。有意思的问题可以直接当小标题。
- 删掉“轻松掌握”“一眼看懂”“至关重要”一类宣传句，也少用“主线已经连起来”“不影响本章的完成”这类课程管理句。
- 不每页都套“接口清单 → 运行与核对 → 测试覆盖 → 参考资料”的模板。页面的结构由当前问题决定。
- 易读不等于每句话都另起一段。同一个问题的现象、原因和结论尽量留在一个句群里；短段只留给真正的转折，不用来逐条播报写作步骤。
- 技术限定放在容易误解的地方。不必在每个直觉后面都跟一串“不代表”“不保证”。
- 保留作者的偏好和口吻，但删掉“恭喜你发明了”、替读者宣告“你已经掌握”等话。
- 不要为了显得生动，把每个标题都写成“怎样……”或“为什么……”。真有问题时就问，其余标题直接说内容。

## 技术和图片

每个数值图都要能从源数据重算。卷积输出、im2col 的行列、池化梯度、head 映射和 causal 性质均应有自动检查。生成式配图只负责视觉呈现，图上的矩阵、公式和箭头要逐项审核。

不把 smoke test、合成数据或几步训练写成模型能力。引用外部 checkpoint 或实验结果时，正文直接给出来源和评估数据集。

## 发布前检查

```bash
python -m unittest tests.test_docs tests.test_site -v
python scripts/build_docs_site.py
hugo --source site --minify
python scripts/check_site_links.py
```

`chapters/` 或实现 README 更新后，GitHub Pages workflow 会再跑一次这些检查。`site/content`、`site/public` 和文档配图副本是生成物，不进入版本库。

修改 Python 实现时，还需运行 `python -m unittest discover -s tests -v`。`tests.yml` 会在 Python 3.10 和 3.12 的 CPU 环境检查损失、梯度、数据隔离及训练恢复；这些测试不下载 MNIST 或 CIFAR-100。

网页的复制、搜索、手机目录和全部交互演示使用真实浏览器检查：

```bash
npm ci
npx playwright install chromium
npm run test:site
```

这组命令需要本机有 Hugo 和 Python。它把页面构建到被忽略的 `site/.browser-build/`，不会和 `hugo server` 的输出目录争用；搜索索引也在这里生成。GitHub Pages 发布前会运行相同的浏览器检查。标题锚点与页面链接可以一起检查：`python scripts/check_site_links.py --site site/.browser-build`。

网页标题写给读者，不显示 `task_20` 这类内部编号。顺序由 `site/data/docs.json` 决定，可以与文件编号不同。交互演示沿用正文里的同一组数字；如果换了数据，就在旁边说明。

公式符号和行内 shape 的悬停解释收录在 `site/static/symbol-data.js`。新配图在图上标了参数时，可在同一文件的 `figures` 中登记图例；`tests/test_site.py` 会检查图片与图例是否对得上。
