# 教程改稿 workflow

这套教程有两种页面，各自承担不同角色：

- `chapters/` 是可以从头顺着读的主线。它先建立问题和直觉，再引出数学与实现。
- `exercises/*/README.md` 是实现手册。它保留完整 shape、接口、命令、预期输出和测试边界。

同一概念在主线中解释“为什么出现”，在实现页中说明“代码怎样工作”，不在两处重复堆放同一组清单。

## 一节内容的顺序

新增或重写一节时，按下面的顺序检查：

1. **承接**：上一种方法在哪个具体例子上不够用？
2. **最小例子**：先给一个能手算或能画出来的例子。
3. **数学**：公式中的每个量都已经在前文出现，不突然引入符号。
4. **shape**：从单样本走到 batch，再落到仓库采用的轴顺序。
5. **代码落点**：链接到真实文件，说明 forward、缓存、backward 或状态对应哪里。
6. **可观察现象**：给出数值、曲线、性质或失败方式，不用空泛的“理解”“掌握”。
7. **过渡**：本节留下的缺口自然引出下一节。

这不是固定标题模板。一段叙事只要完成了这些连接，就不必机械地写成七个小节。

## 语气

- 使用“为什么会需要它”“运行时会看到什么”这类描述，少用命令式口吻。
- 不布置作业，不要求提交截图、报告或代码。
- 不用“轻松掌握”“从零到精通”“一眼看懂”等宣传句。
- 类比用于建立第一层直觉，随后要回到公式、shape 或代码。
- 不把 smoke test、合成数据或短训练描述成真实任务上的模型能力。

## 本地检查

```bash
python -m unittest tests.test_docs tests.test_site -v
python scripts/build_docs_site.py
hugo --source site --minify
python scripts/check_site_links.py
```

`chapters/` 或练习 README 更新后，GitHub Pages workflow 会重复以上文档检查，再部署 Hugo 输出。生成的 `site/content`、`site/public` 和文档配图副本不会进入版本库。
